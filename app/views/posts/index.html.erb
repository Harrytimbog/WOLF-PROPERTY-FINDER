<p id="notice"><%= notice %></p>

<div class="container">
  <div class="row">
    <div class="col-md-8">
      <h3 class="text-center">Posts</h3>

      <table class="table table-striped">
        <thead>
          <tr>
            <th class="text-center">Title</th>
          </tr>
        </thead>

        <tbody>
          <% @posts.each do |post| %>
            <tr>
              <td class="mr-5"><%= post.title %> <%= link_to 'Show', post %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <br>

      <%= link_to 'New Post', new_post_path %>
    </div>
  </div>
</div>
